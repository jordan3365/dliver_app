name: driver_flutter
description: "Coupang Flex Style TMS Delivery Driver App in Flutter"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.5
  http: ^1.2.0
  shared_preferences: ^2.2.0
  geolocator: ^11.0.0
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  flutter_tts: ^3.8.5
  image_picker: ^1.0.7
  url_launcher: ^6.2.4
  signature: ^5.4.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
